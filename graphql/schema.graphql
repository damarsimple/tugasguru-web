#import types.graphql
#import student/*.graphql
#import teacher/*.graphql
#import user/*.graphql

type Query {
    provinces: [Province!]! @all
    cities: [City!]! @all
    me: User @auth
    accesses: [Access!]! @all
}
